export const metadata = {
  title: 'List of commands',
  description:
    'The list of built-in commands you can bind to keystrokes or invoke from plugins.',
}

# List of commands

Inkdrop provides a bunch of commands to allow you to associate arbitrary interactions with the features.
You can bind them with [keystrokes](/modules/keymap-manager), [menu](/modules/menu-manager) and [context menu](/modules/context-menu-manager) from [plugins](/guides/plugin-word-count), [the init file](/guides/the-init-file) or [keymap config file](https://docs.inkdrop.app/manual/customizing-keybindings).
Note that some commands like `core:open-note` require parameters so you have to invoke them programmatically with [CommandRegistry](/modules/command-registry) like so:

```js
inkdrop.commands.dispatch(document.body, 'core:open-note', {
  noteId: 'note:uugSvPq5r'
})
```

## Table of Contents

- [Application commands](#application-commands)
- [Core commands](#core-commands)
- [Telescope commands](#telescope-commands)
- [Sidebar commands](#sidebar-commands)
- [Editor commands](#editor-commands)
- [AI commands](#ai-commands)
- [Table editor commands](#table-editor-commands)
- [Export commands](#export-commands)
- [Import commands](#import-commands)
- [View commands](#view-commands)
- [Window commands](#window-commands)
- [Special commands](#special-commands)

---

## Application commands

Special commands that run in the main process to control the Inkdrop app.
To invoke them, do like so:

```js
import { ipcRenderer } from 'electron'
ipcRenderer.send('command', 'application:about', {})
```

### application:about

Shows a dialog about Inkdrop.

### application:bring-all-windows-to-front {{ warning: 'macOS only' }}

Arranges windows listed in the Window menu in front of all other windows.

### application:check-for-update

Checks for available app updates.

### application:cycle-through-windows {{ warning: 'macOS only' }}

Cycles through open Inkdrop windows.

### application:hide {{ warning: 'macOS only' }}

Hides Inkdrop windows.

### application:hide-other-applications {{ warning: 'macOS only' }}

Hides all apps, except Inkdrop.

### application:inspect

Shows a Developer Tools and inspects an element.

### application:logout

Logs out from the currently authenticated account.

### application:minimize {{ warning: 'macOS only' }}

Minimizes Inkdrop application.

### application:open-preferences

Opens up the preference window.

### application:open-website

Opens up the Inkdrop website.

### application:open-website-signup

Opens up the Inkdrop sign-up page.

### application:quit

Quits the app.

### application:report-issue

Opens up the Inkdrop forum.

### application:show-credits

Opens up the list of software licenses.

### application:unhide-all-applications {{ warning: 'macOS only' }}

Unhides all apps, including Inkdrop.

### application:view-help

Opens up the user manual.

### application:view-api-doc

Opens up the API documentation website.

### application:zoom {{ warning: 'macOS only' }}

Toggles the size and location of the window between its standard state and its user state.

### application:sync-db

Perform data sync from the last checkpointer.

### application:quick-note

Create a note and show it in a separate window.

---

## Core commands

### core:activate-package {{ selector: 'body' }}

Activates the named package.

<Properties sub title="Arguments">
  <Property name="name" type="string" required>
    The package name.
  </Property>
</Properties>

### core:activate-themes {{ selector: 'body' }}

Re-applies the currently selected UI and syntax themes.

### core:choose-template {{ selector: 'body' }}

Opens the template picker to apply a template to the current note.
Only available when the editing note is empty and is not in Templates or Trash.

### core:copy {{ selector: 'body' }}

Copy the selected text to clipboard.

### core:copy-image {{ selector: 'body' }}

Copy the target element's image to clipboard.

### core:copy-note-link {{ selector: 'body' }}

Copy links to notes as Markdown links. By default, copy currently selected on the note list.

<Properties sub title="Arguments">
  <Property name="noteId" type="string|array">
    (Optional) The ID(s) of the note(s) to process.
  </Property>
</Properties>

### core:copy-note-uri {{ selector: 'body' }}

Copy `inkdrop://` URIs to notes. By default, copy currently selected on the note list.

<Properties sub title="Arguments">
  <Property name="noteId" type="string|array">
    (Optional) The ID(s) of the note(s) to process.
  </Property>
</Properties>

### core:copy-link-address {{ selector: 'body' }}

Copy the link address of an `<a>` element under the event target in the preview pane.

### core:copy-preview-selection {{ selector: 'body' }}

Copy the currently selected text in the preview pane to the clipboard.

### core:copy-note-id {{ selector: 'body' }}

Copies the IDs of the selected notes to the clipboard, one per line.

<Properties sub title="Arguments">
  <Property name="noteId" type="string | string[]">
    The note ID(s) to copy. Defaults to the notes selected on the note list bar.
  </Property>
</Properties>

### core:copy-notebook-id {{ selector: 'body' }}

Copies a notebook ID to the clipboard.

<Properties sub title="Arguments">
  <Property name="bookId" type="string">
    The notebook ID to copy. Defaults to the target element's `data-bookid`, then to the notebook targeted by the context menu.
  </Property>
</Properties>

### core:copy-tag-id {{ selector: 'body' }}

Copies a tag ID to the clipboard.

<Properties sub title="Arguments">
  <Property name="tagId" type="string">
    The tag ID to copy. Defaults to the target element's `data-tagid`, then to the tag targeted by the context menu.
  </Property>
</Properties>

### core:cut {{ selector: 'body' }}

Delete the selected text and copy it to clipboard.

### core:deactivate-package {{ selector: 'body' }}

Deactivates the named package, if it is active.

<Properties sub title="Arguments">
  <Property name="name" type="string" required>
    The package name.
  </Property>
</Properties>

### core:delete-note {{ selector: 'body' }}

Move notes currently selected on the note list to trash.
If the notes are already in trash, they will be permanently deleted.

### core:delete-notebook

<Warning>It's a private command for now</Warning>

### core:delete-tag

<Warning>It's a private command for now</Warning>

### core:duplicate-note {{ selector: 'body' }}

Duplicates notes currently selected on the note list.

### core:emphasize {{ selector: '.cm-editor' }}

Emphasize currently selected text.

### core:empty-trash {{ selector: 'body' }}

Empties trash.

### core:filter-notes {{ selector: 'body' }}

Filters current note list items with given keywords.

<Properties sub title="Arguments">
  <Property name="keyword" type="string" required>
    The keywords to filter.
  </Property>
</Properties>

### core:filter-by-tag {{ selector: 'body' }}

Adds a filter condition with specified tag.

<Properties sub title="Arguments">
  <Property name="tagId" type="string" required>
    The ID of the tag to filter by.
  </Property>
</Properties>

### core:find {{ selector: 'body' }}

Activates the search bar.

### core:find-focus {{ selector: 'body' }}

Moves focus to the search bar.

### core:find-clear {{ selector: 'body' }}

Clears the search bar text.

### core:find-global {{ selector: 'body' }}

Moves focus to the search bar and switch the search scope to global.

<Properties sub title="Arguments">
  <Property name="focus" type="boolean">
    Whether to move focus to the search bar. Default: `true`.
  </Property>
</Properties>

### core:focus-next {{ selector: 'body' }}

Moves focus to the next focusable UI component.

### core:focus-note-list-bar {{ selector: 'body' }}

Moves focus to the note list bar.

### core:focus-previous {{ selector: 'body' }}

Moves focus to the previous focusable UI component.

### core:indent {{ selector: '.cm-editor' }}

Indents the selected line(s).

### core:insert-code-block {{ selector: '.cm-editor' }}

Inserts a code block.

### core:insert-horizontal-rule {{ selector: '.cm-editor' }}

Inserts a horizontal rule: `* * *`.

### core:insert-link {{ selector: '.cm-editor' }}

Inserts a link.

### core:insert-image {{ selector: '.cm-editor' }}

Shows a file picker and inserts the selected image into the editor at the cursor position.

### core:move-notebook

<Warning>It's a private command for now</Warning>

### core:move-to-notebook

<Warning>It's a private command for now</Warning>

### core:navigate {{ selector: 'body' }}

Moves through the note open history in the given direction.
Dispatches `core:navigate-back` or `core:navigate-forward` accordingly.

<Properties sub title="Arguments">
  <Property name="direction" type="string" required>
    Either `"back"` or `"forward"`. Default: `"back"`
  </Property>
</Properties>

### core:navigate-back {{ selector: 'body' }}

Moves backward through the note open history.

### core:navigate-forward {{ selector: 'body' }}

Moves forward through the note open history.

### core:new-note {{ selector: 'body' }}

Creates a new note and open it in the editor.

<Properties sub title="Arguments">
  <Property name="bookId" type="string">
    (Optional) The notebook ID to create the note in.
  </Property>
</Properties>

### core:new-note-in-separate-window {{ selector: 'body' }}

Creates a new note in a separate window. Uses the currently selected notebook if applicable.

### core:new-note-from-selection {{ selector: 'body' }}

Creates a new note from the selected text on the editor.

### core:new-note-from-link {{ selector: 'body' }}

Creates a new note from a link that targets an unresolved note in the preview pane.

<Properties sub title="Arguments">
  <Property name="title" type="string" required>
    The title of the new note (also used as the link label).
  </Property>
  <Property name="line" type="number" required>
    The line of the editor where the link appears.
  </Property>
  <Property name="newWindow" type="boolean">
    Whether to open the new note in a separate window.
  </Property>
</Properties>

### core:new-notebook {{ selector: 'body' }}

Shows up a dialog for creating a new notebook.

<Properties sub title="Arguments">
  <Property name="parentBookId" type="string">
    A parent notebook ID. If `undefined`, a notebook will be created under the root. Default: `undefined`
  </Property>
</Properties>

### core:note-list-show-all-notes {{ selector: 'body' }}

Lists all notes on the note list bar.

<Properties sub title="Arguments">
  <Property name="selectFirstNote" type="boolean">
    Whether to select the first note or not. Default: `true`
  </Property>
</Properties>

### core:note-list-show-notes-in-templates {{ selector: 'body' }}

Lists note templates on the note list bar, and closes the currently open note.

<Properties sub title="Arguments">
  <Property name="callback" type="function">
    Invoked after the note list has finished reloading.
  </Property>
</Properties>

### core:note-list-show-notes-in-trash {{ selector: 'body' }}

Lists notes in the trash on the note list bar.

<Properties sub title="Arguments">
  <Property name="selectFirstNote" type="boolean">
    Whether to select the first note or not. Default: `true`
  </Property>
</Properties>

### core:note-list-show-notes-in-book {{ selector: 'body' }}

List notes in specified notebook on the note list bar

<Properties sub title="Arguments">
  <Property name="bookId" type="string" required>
    The notebook ID
  </Property>
  <Property name="tagId" type="string">
    The tag ID to filter items
  </Property>
  <Property name="status" type="NoteStatus">
    The note status which should be **one of:**`"none"` or `"active"` or `"onHold"` or `"completed"` or `"dropped"`
  </Property>
  <Property name="selectFirstNote" type="boolean">
    Whether to select the first note or not. Default: `true`
  </Property>
  <Property name="includeChildren" type="boolean">
    Whether to include the notes in the child notebooks. Default: `false`
  </Property>
</Properties>

### core:note-list-show-notes-with-tag {{ selector: 'body' }}

List notes with specified tag on the note list bar

<Properties sub title="Arguments">
  <Property name="tagId" type="string" required>
    The tag ID.
  </Property>
  <Property name="selectFirstNote" type="boolean">
    Whether to select the first note or not. Default: `true`.
  </Property>
</Properties>

### core:note-list-show-notes-with-status {{ selector: 'body' }}

List notes with specified note status on the note list bar

<Properties sub title="Arguments">
  <Property name="status" type="NoteStatus" required>
    The note status which should be **one of:** `"none"` or `"active"` or `"onHold"` or `"completed"` or `"dropped"`.
  </Property>
  <Property name="selectFirstNote" type="boolean">
    Whether to select the first note or not. Default: `true`.
  </Property>
</Properties>

### core:note-tags-bar-item-list-unselect {{ selector: '.note-tags-bar-item-list' }}

Unselect items on the note tags bar.

### core:note-tags-bar-item-list-select-prev {{ selector: '.note-tags-bar-item-list' }}

Select the previous item on the note tags bar.

### core:note-tags-bar-item-list-select-next {{ selector: '.note-tags-bar-item-list' }}

Select the next item on the note tags bar.

### core:note-tags-bar-item-list-remove-item-before {{ selector: '.note-tags-bar-item-list' }}

Remove the selected item and select its previous item on the note tags bar.

### core:note-tags-bar-item-list-remove-item {{ selector: '.note-tags-bar-item-list' }}

Remove the target item on the note tags bar.

<Properties sub title="Arguments">
  <Property name="tagId" type="string">
    (Optional) The tag ID to remove.
  </Property>
</Properties>

### core:open-first-note {{ selector: 'body' }}

Opens the first note of the note list bar in the editor.

### core:open-next-note {{ selector: 'body' }}

Opens the next note of currently selected note on the note list bar.

### core:open-prev-note {{ selector: 'body' }}

Opens the previous note of currently selected note on the note list bar.

### core:close-note {{ selector: 'body' }}

Closes the currently editing note.

### core:open-note {{ selector: 'body' }}

Opens a note with specified note ID.

<Properties sub title="Arguments">
  <Property name="noteId" type="string" required>
    The ID of the note to open.
  </Property>
  <Property name="revision" type="string">
    (Optional) Open the specific revision of the note.
  </Property>
  <Property name="selectInNoteListBar" type="boolean">
    Whether to select the correspond note in the note list bar. Default: `true`.
  </Property>
  <Property name="pushState" type="boolean">
    Remember it in the navigation history. Default: `true`.
  </Property>
  <Property name="newWindow" type="boolean">
    Whether to open the note in a separate window. Default: `false`.
  </Property>
  <Property name="viewMode" type="EditorViewMode">
    The editor view mode to switch to after opening. One of `"edit"`, `"preview"`, or `"sidebyside"`.
  </Property>
  <Property name="focus" type="boolean">
    Whether to focus the editor after opening. Default: `false`.
  </Property>
</Properties>

### core:open-note-in-separate-window {{ selector: 'body' }}

Opens notes in separate windows. By default, opens notes selected on the note list bar.

<Properties sub title="Arguments">
  <Property name="noteId" type="string|array">
    (Optional) The ID(s) of the note(s) to open.
  </Property>
</Properties>

### core:open-tag-settings {{ selector: 'body' }}

Opens up the settings window for the tag with specified ID.

<Properties sub title="Arguments">
  <Property name="tagId" type="string">
    The ID of the tag to set.
  </Property>
</Properties>

### core:open-template {{ selector: 'body' }}

Opens a note template in the editor.
A template that already exists in the database is opened like a normal note; an official template that has not been saved yet is loaded into the editor unsaved.

<Properties sub title="Arguments">
  <Property name="template" type="Note" required>
    The template note to open.
  </Property>
</Properties>

### core:paste {{ selector: 'body' }}

Pastes a text from clipboard.

### core:paste-as-plain-text {{ selector: 'body' }}

Pastes a text from clipboard as plain text.

### core:pin-note-to-top {{ selector: 'body' }}

Pin or unpin notes to the top of the note list. By default, applies to notes currently selected on the note list.

<Properties sub title="Arguments">
  <Property name="noteId" type="string|array">
    (Optional) The ID(s) of the note(s) to pin or unpin.
  </Property>
  <Property name="pinned" type="boolean">
    (Optional) The desired pinned state. If omitted, toggles the current state of the first target note.
  </Property>
</Properties>

### core:popup-app-menu {{ selector: 'body' }}

Shows the application menu as a popup context menu (useful on Windows / Linux where the menu bar is hidden).

### core:redo {{ selector: 'body' }}

Redoes the last undone change.

### core:reload-package {{ selector: 'body' }}

Unloads and re-loads the named package, if it is loaded.

<Properties sub title="Arguments">
  <Property name="name" type="string" required>
    The package name.
  </Property>
</Properties>

### core:rename-notebook

<Warning>It's a private command for now</Warning>

### core:save-image {{ selector: 'body' }}

Saves the target element's image to disk.

### core:save-note {{ selector: 'body' }}

Saves the currently editing note to the local database.

### core:select-all {{ selector: 'body' }}

Select all text.

### core:search-notes {{ selector: 'body' }}

Search notes globally with given keywords.

<Properties sub title="Arguments">
  <Property name="keyword" type="string" required>
    The keywords to search.
  </Property>
</Properties>

### core:share-note {{ selector: 'body' }}

Shows up a dialog for sharing note with specified ID.

<Properties sub title="Arguments">
  <Property name="noteId" type="string" required>
    The ID of the note to be shared.
  </Property>
</Properties>

### core:show-note-revisions {{ selector: 'body' }}

Shows up a dialog for viewing note change history with specified ID.

<Properties sub title="Arguments">
  <Property name="noteId" type="string" required>
    The ID of the note to view its revision history.
  </Property>
</Properties>

### core:show-editor-action-menu {{ selector: 'body' }}

Opens the editor's action menu below the header button, with the per-note actions (move, duplicate, copy link, pin, revisions, share, delete).

### core:show-image-viewer {{ selector: 'body' }}

Opens the full-screen image viewer for the given image.

<Properties sub title="Arguments">
  <Property name="src" type="string" required>
    The image URL or URI to display.
  </Property>
  <Property name="alt" type="string">
    The alt text to show as a caption.
  </Property>
</Properties>

### core:strikethrough {{ selector: '.cm-editor' }}

Toggles strikethrough to the selected text.

### core:strong {{ selector: '.cm-editor' }}

Toggles strong emphasis to the selected text.

### core:toggle-blockquote {{ selector: '.cm-editor' }}

Toggles blockquote to the selected lines.

### core:toggle-bulleted-list {{ selector: '.cm-editor' }}

Toggles bulleted list to the selected lines.

### core:toggle-heading-1 {{ selector: '.cm-editor' }}

Toggles h1 heading to the selected lines.

### core:toggle-heading-2 {{ selector: '.cm-editor' }}

Toggles h2 heading to the selected lines.

### core:toggle-heading-3 {{ selector: '.cm-editor' }}

Toggles h3 heading to the selected lines.

### core:toggle-heading-4 {{ selector: '.cm-editor' }}

Toggles h4 heading to the selected lines.

### core:toggle-heading-bigger {{ selector: '.cm-editor' }}

Decreases heading level of the selected lines.

### core:toggle-heading-smaller {{ selector: '.cm-editor' }}

Increases heading level of the selected lines.

### core:toggle-numbered-list {{ selector: '.cm-editor' }}

Toggles ordered list to the selected lines.

### core:toggle-task-list {{ selector: '.cm-editor' }}

Toggles task list to the selected lines.

### core:toggle-inline-code {{ selector: '.cm-editor' }}

Toggles inline code block to the selected text.

<Properties sub title="Arguments">
  <Property name="selectionOnly" type="boolean">
    Whether to limit the toggle to the current selection only.
  </Property>
</Properties>

### core:toggle-inline-mark-tag {{ selector: '.cm-editor' }}

Toggles an inline `<mark>` tag around the selected text.

### core:undo {{ selector: '.cm-editor' }}

Undoes the last change.

### core:unindent {{ selector: '.cm-editor' }}

Unindents the current lines.

### core:unload-package {{ selector: 'body' }}

Unloads the named package from memory, if it is loaded.

<Properties sub title="Arguments">
  <Property name="name" type="string" required>
    The package name.
  </Property>
</Properties>

### core:insert-alert {{ selector: '.cm-editor' }}

Inserts an alert block with the specified type.

<Properties sub title="Arguments">
  <Property name="type" type="string" required>
    The type of the alert block. It should be one of "NOTE", "TIP", "IMPORTANT", "WARNING", or "CAUTION".
  </Property>
</Properties>

### core:insert-alert-note {{ selector: '.cm-editor' }}

Inserts a note alert block.

### core:insert-alert-tip {{ selector: '.cm-editor' }}

Inserts a tip alert block.

### core:insert-alert-important {{ selector: '.cm-editor' }}

Inserts an important alert block.

### core:insert-alert-warning {{ selector: '.cm-editor' }}

Inserts a warning alert block.

### core:insert-alert-caution {{ selector: '.cm-editor' }}

Inserts a caution alert block.

---

## Telescope commands

[Telescope](https://docs.inkdrop.app/manual/quick-finder) is the quick finder for commands, notebooks, tags, and more.

### core:toggle-telescope {{ selector: 'body' }}

Opens Telescope if it is closed, or closes it if it is open.

### core:show-telescope {{ selector: 'body' }}

Opens Telescope.

<Properties sub title="Arguments">
  <Property name="scopedSourceId" type="string">
    (Optional) Restrict the search to a specific source (e.g. commands, notebooks, tags).
  </Property>
  <Property name="scopedWorkspaceId" type="string|null">
    (Optional) Restrict the search to a specific workspace.
  </Property>
  <Property name="initialSelectedItemId" type="string|null">
    (Optional) The initially selected item.
  </Property>
  <Property name="cancelBehavior" type="string">
    (Optional) Behavior when canceling, e.g. `"close"`.
  </Property>
</Properties>

### core:close-telescope {{ selector: 'body' }}

Closes Telescope.

### core:telescope-clear-query {{ selector: 'body' }}

Clears the Telescope query input.

### core:telescope-clear-active-source {{ selector: 'body' }}

Clears the active scoped source in Telescope.

### core:telescope-commands {{ selector: 'body' }}

Opens Telescope scoped to the commands source.

### core:telescope-notebooks {{ selector: 'body' }}

Opens Telescope scoped to the notebooks source.

### core:telescope-tags {{ selector: 'body' }}

Opens Telescope scoped to the tags source.

---

## Sidebar commands

### core:sidebar-focus {{ selector: 'body' }}

Moves focus to the side bar.

### core:sidebar-select-next-item {{ selector: 'body' }}

Select the next item of the side bar menu.

### core:sidebar-select-prev-item {{ selector: 'body' }}

Select the previous item of the side bar menu.

### core:sidebar-select-item {{ selector: 'body' }}

Activate the currently selected item.

### core:sidebar-disclose-item {{ selector: 'body' }}

Toggle the disclosure (expand/collapse) of the currently selected item.

### core:sidebar-expand-item {{ selector: 'body' }}

Expand the currently selected item.

### core:sidebar-collapse-item {{ selector: 'body' }}

Collapse the currently selected item.

### core:sidebar-scroll-to-selection {{ selector: 'body' }}

Scrolls the side bar to the currently selected item.

### core:sidebar-open-workspace-menu {{ selector: 'body' }}

Open the workspace menu for a notebook.

<Properties sub title="Arguments">
  <Property name="bookId" type="string">
    (Optional) The notebook ID. If omitted, opens the current workspace.
  </Property>
  <Property name="selectFirstNote" type="boolean">
    Whether to select the first note in the workspace. Default: `true`.
  </Property>
</Properties>

### core:sidebar-hide-workspace-menu {{ selector: 'body' }}

Hide the workspace menu.

### core:sidebar-workspace-go-up {{ selector: 'body' }}

Navigate one level up in the workspace menu.

---

## Editor commands

### editor:clear-find {{ selector: 'body' }}

Clears the text in the editor search bar.

<Properties sub title="Arguments">
  <Property name="focus" type="boolean">
    Whether to keep the search bar focused after clearing.
  </Property>
</Properties>

### editor:toggle-regex {{ selector: '.cm-editor' }}

Toggles regex matching in the editor search bar.

### editor:toggle-case-sensitive {{ selector: '.cm-editor' }}

Toggles case-sensitive matching in the editor search bar.

### editor:change-note-status-none {{ selector: 'body' }}

Set the note status of the editing note to None.

### editor:change-note-status-active {{ selector: 'body' }}

Set the note status of the editing note to Active.

### editor:change-note-status-onhold {{ selector: 'body' }}

Set the note status of the editing note to OnHold.

### editor:change-note-status-completed {{ selector: 'body' }}

Set the note status of the editing note to Completed.

### editor:change-note-status-dropped {{ selector: 'body' }}

Set the note status of the editing note to Dropped.

### editor:copy-line-up {{ selector: '.cm-editor' }}

Duplicates the selected lines upward, keeping the selection in the top copy.

### editor:copy-line-down {{ selector: '.cm-editor' }}

Duplicates the selected lines downward, keeping the selection in the bottom copy.

### editor:delete-char-after {{ selector: '.cm-editor' }}

Deletes the char after the cursor.

### editor:delete-char-before {{ selector: '.cm-editor' }}

Deletes the char before the cursor.

### editor:delete-group-after {{ selector: '.cm-editor' }}

Deletes the text group after the cursor.

### editor:delete-group-before {{ selector: '.cm-editor' }}

Deletes the text group before the cursor.

### editor:delete-line {{ selector: '.cm-editor' }}

Deletes the line.

### editor:delete-word-after {{ selector: '.cm-editor' }}

Deletes the word after the cursor.

### editor:delete-word-before {{ selector: '.cm-editor' }}

Deletes the word before the cursor.

### editor:delete-wrapped-line-left {{ selector: '.cm-editor' }}

Deletes the wrapped line left.

### editor:delete-wrapped-line-right {{ selector: '.cm-editor' }}

Deletes the wrapped line right.

### editor:upcase-at-cursor {{ selector: '.cm-editor' }}

Uppercases the selection, or the word at the cursor when nothing is selected.

### editor:downcase-at-cursor {{ selector: '.cm-editor' }}

Lowercases the selection, or the word at the cursor when nothing is selected.

### editor:edit-with-ai {{ selector: '.cm-editor' }}

Opens the AI inline assistant to edit the currently selected text. Does nothing if there is no selection.

### editor:trigger-nes {{ selector: '.cm-editor' }}

Requests a Next Edit Suggestion at the cursor.
Suggestions are rendered as dim ghost text and can also be triggered automatically after an idle pause, depending on the `ai.nextEditSuggestions` config.

### editor:accept-nes {{ selector: '.cm-editor' }}

Accepts the visible Next Edit Suggestion, inserting the ghost text as a single undoable edit.
Does nothing and lets the keystroke fall through when no suggestion is visible.

### editor:dismiss-nes {{ selector: '.cm-editor' }}

Dismisses the visible Next Edit Suggestion.

### editor:find {{ selector: 'body' }}

Toggles the editor search bar.

### editor:find-next {{ selector: 'body' }}

Locates the next item in the editor that matches criteria.

### editor:find-prev {{ selector: 'body' }}

Locates the previous item in the editor that matches criteria.

### editor:find-text {{ selector: 'body' }}

Finds given text in the editor.

<Properties sub title="Arguments">
  <Property name="text" type="string" required>
    The text to find.
  </Property>
  <Property name="parseAsQuery" type="boolean">
    Whether to parse the text as a search query (with operators).
  </Property>
</Properties>

### editor:find-under {{ selector: '.cm-editor' }}

Selects the next occurrence of the selection, or of the word at the cursor when nothing is selected, wrapping around the end of the note.

### editor:find-under-previous {{ selector: '.cm-editor' }}

Selects the previous occurrence of the selection, or of the word at the cursor when nothing is selected, wrapping around the start of the note.

### editor:focus {{ selector: 'body' }}

Focuses to the MDE or preview pane depending on the editor mode.

### editor:focus-mde {{ selector: 'body' }}

Moves focus to the MDE.

### editor:focus-preview {{ selector: 'body' }}

Moves focus to the preview pane.

### editor:go-char-left {{ selector: '.cm-editor' }}

Moves the cursor to one character left.

### editor:go-char-right {{ selector: '.cm-editor' }}

Moves the cursor to one character right.

### editor:go-doc-end {{ selector: '.cm-editor' }}

Moves the cursor to the end of the document.

### editor:go-doc-start {{ selector: '.cm-editor' }}

Moves the cursor to the start of the document.

### editor:go-group-left {{ selector: '.cm-editor' }}

Moves the cursor to one group left.

### editor:go-group-right {{ selector: '.cm-editor' }}

Moves the cursor to one group right.

### editor:go-line-down {{ selector: '.cm-editor' }}

Moves the cursor to one line down.

### editor:go-line-end {{ selector: '.cm-editor' }}

Moves the cursor to the line end.

### editor:go-line-left {{ selector: '.cm-editor' }}

Moves the cursor to the line left.

### editor:go-line-right {{ selector: '.cm-editor' }}

Moves the cursor to the line right.

### editor:go-line-start {{ selector: '.cm-editor' }}

Moves the cursor to the line start.

### editor:go-line-up {{ selector: '.cm-editor' }}

Moves the cursor to one line up.

### editor:go-matching-bracket {{ selector: '.cm-editor' }}

Moves the cursor to the bracket matching the one it is on.

### editor:go-page-down {{ selector: '.cm-editor' }}

Moves the cursor to one page down.

### editor:go-page-up {{ selector: '.cm-editor' }}

Moves the cursor to one page up.

### editor:go-subword-left {{ selector: '.cm-editor' }}

Moves the cursor one group or camel-case subword backward.

### editor:go-subword-right {{ selector: '.cm-editor' }}

Moves the cursor one group or camel-case subword forward.

### editor:go-syntax-left {{ selector: '.cm-editor' }}

Moves the cursor one syntax node to the left.

### editor:go-syntax-right {{ selector: '.cm-editor' }}

Moves the cursor one syntax node to the right.

### editor:select-char-left {{ selector: '.cm-editor' }}

Extends the selection by one character to the left.

### editor:select-char-right {{ selector: '.cm-editor' }}

Extends the selection by one character to the right.

### editor:select-syntax-left {{ selector: '.cm-editor' }}

Extends the selection one syntax node to the left.

### editor:select-syntax-right {{ selector: '.cm-editor' }}

Extends the selection one syntax node to the right.

### editor:select-subword-left {{ selector: '.cm-editor' }}

Moves the selection head one group or camel-case subword backward.

### editor:select-subword-right {{ selector: '.cm-editor' }}

Moves the selection head one group or camel-case subword forward.

### editor:select-group-left {{ selector: '.cm-editor' }}

Extends the selection by one group to the left.

### editor:select-group-right {{ selector: '.cm-editor' }}

Extends the selection by one group to the right.

### editor:select-line-up {{ selector: '.cm-editor' }}

Extends the selection one line up.

### editor:select-line-down {{ selector: '.cm-editor' }}

Extends the selection one line down.

### editor:select-line-start {{ selector: '.cm-editor' }}

Extends the selection to the line start.

### editor:select-line-end {{ selector: '.cm-editor' }}

Extends the selection to the line end.

### editor:select-line-left {{ selector: '.cm-editor' }}

Extends the selection to the line boundary on the left.

### editor:select-line-right {{ selector: '.cm-editor' }}

Extends the selection to the line boundary on the right.

### editor:select-page-up {{ selector: '.cm-editor' }}

Extends the selection one page up.

### editor:select-page-down {{ selector: '.cm-editor' }}

Extends the selection one page down.

### editor:select-doc-start {{ selector: '.cm-editor' }}

Extends the selection to the start of the document.

### editor:select-doc-end {{ selector: '.cm-editor' }}

Extends the selection to the end of the document.

### editor:select-line {{ selector: '.cm-editor' }}

Expands the selection to cover entire lines.

### editor:select-parent-syntax {{ selector: '.cm-editor' }}

Expands the selection to the next larger syntactic construct.

### editor:select-matching-bracket {{ selector: '.cm-editor' }}

Extends the selection to the bracket matching the one the selection head is on.

### editor:select-next-occurrence {{ selector: '.cm-editor' }}

Adds a selection at the next occurrence of the selected text, or selects the word at the cursor when the selection is empty.

### editor:select-selection-matches {{ selector: '.cm-editor' }}

Selects all instances of the currently selected text.

### editor:skip-and-select-next-occurrence {{ selector: '.cm-editor' }}

Moves the selection on to the next occurrence instead of adding it, dropping the range it searched from.
With nothing selected, the first press only expands the selection to the word at the cursor.

### editor:split-selection-by-line {{ selector: '.cm-editor' }}

Replaces every selection range with one range per line it covers.

### editor:move-line-up {{ selector: '.cm-editor' }}

Moves the current line(s) up.

### editor:move-line-down {{ selector: '.cm-editor' }}

Moves the current line(s) down.

### editor:indent {{ selector: '.cm-editor' }}

Indents the currently selected line(s).

### editor:indent-less {{ selector: '.cm-editor' }}

Unindents the currently selected line(s).

### editor:indent-more {{ selector: '.cm-editor' }}

Indents one more the currently selected line(s).

### editor:insert-blank-line {{ selector: '.cm-editor' }}

Creates a blank, indented line below the current line.

### editor:insert-line-before {{ selector: '.cm-editor' }}

Opens an indented blank line above the line at the cursor and moves the cursor onto it — the upward counterpart of `editor:insert-blank-line`.

### editor:insert-images {{ selector: 'body' }}

Inserts image files at the cursor to the editing note.

### editor:insert-link {{ selector: 'body' }}

Opens the "Insert a link" dialog, pre-filled with the given title and URL, and inserts `[title](url)` at the cursor.
Not registered while the session is read-only.

<Properties sub title="Arguments">
  <Property name="title" type="string">
    The link title to pre-fill.
  </Property>
  <Property name="url" type="string">
    The URL to pre-fill. When given, the URL field is selected instead of the
    title field.
  </Property>
</Properties>

### editor:resize-image-auto {{ selector: '.cm-editor' }}

Restores the image at the cursor to its natural size, by removing the `w:` and `h:` tokens from its alt text.
Does nothing when the cursor is not inside an image.

### editor:resize-image-small {{ selector: '.cm-editor' }}

Resizes the image at the cursor to 30% width.

### editor:resize-image-half {{ selector: '.cm-editor' }}

Resizes the image at the cursor to 50% width.

### editor:resize-image-full {{ selector: '.cm-editor' }}

Resizes the image at the cursor to 100% width.

<Row>
  <Col>

    <Properties sub title="Arguments">
      <Property name="pos" type="object">
        Position to insert with the following keys:

        <Properties sub>
          <Property name="ch" type="number">
            Character position.
          </Property>
          <Property name="line" type="number">
            Line position.
          </Property>
        </Properties>

      </Property>
      <Property name="files" type="FileList|array">
        [FileList](https://developer.mozilla.org/en-US/docs/Web/API/FileList), or an Array of [File](https://developer.mozilla.org/en-US/docs/Web/API/File) or [NativeImage](https://electronjs.org/docs/api/native-image) to insert.
      </Property>
    </Properties>

  </Col>
  <Col sticky>
    You can override the default file drop handler like so:

    ```js {{ title: 'Example' }}
    const mde = inkdrop.getActiveEditorOrThrowError()
    inkdrop.commands.add(mde.wrapper.wrapper, {
      'editor:insert-images': e => {
        const { files, pos } = e
        // Do something

        // prevent the default behavior
        e.stopPropagation()
      }
    })
    ```

  </Col>
</Row>

### editor:join-lines {{ selector: '.cm-editor' }}

Joins the lines each selection range covers into a single line, separated by one space and dropping the indentation of the absorbed lines.
An empty selection joins the line at the cursor with the one below it.

### editor:jump-to-line {{ selector: '.cm-editor' }}

Toggles a dialog which allows you to jump to the line at the specified number.

### editor:kill-line {{ selector: '.cm-editor' }}

Deletes from the cursor to the end of the line, in the style of Emacs' `kill-line`.

### editor:new-line {{ selector: '.cm-editor' }}

Inserts a plain new line at the cursor without continuing any Markdown markup.

### editor:new-line-continue-markup {{ selector: '.cm-editor' }}

Inserts a new line and continues the current Markdown markup — list markers, ordered-list numbers, task list checkboxes, and blockquotes — then indents to match the surrounding block.

### editor:open-line {{ selector: '.cm-editor' }}

Inserts a new line above the cursor.

### editor:redo-selection {{ selector: '.cm-editor' }}

Redoes the last selection.

### editor:replace {{ selector: 'body' }}

Initiates the replace sequence in the editor.

### editor:replace-next {{ selector: '.cm-editor' }}

Replaces the next occurrence in the editor.

### editor:replace-all {{ selector: '.cm-editor' }}

Replaces all occurrences in the editor.

### editor:replace-selection {{ selector: '.cm-editor' }}

Replaces selected text with the given text.

<Properties sub title="Arguments">
  <Property name="text" type="string">
    String, the text to replace.
  </Property>
</Properties>

### editor:save-editor-scroll

<Warning>It's a private command</Warning>

### editor:save-preview-scroll

<Warning>It's a private command</Warning>

### editor:scroll-line-up {{ selector: '.cm-editor' }}

Scrolls the viewport up by one line, leaving the cursor where it is.

### editor:scroll-line-down {{ selector: '.cm-editor' }}

Scrolls the viewport down by one line, leaving the cursor where it is.

### editor:show-in-center {{ selector: '.cm-editor' }}

Scrolls the line with the cursor on it to the vertical center of the viewport.

### editor:sort-lines {{ selector: '.cm-editor' }}

Sorts the lines covered by each selection range alphabetically.
Ranges covering fewer than two lines are left alone, so an empty selection never rewrites the note.

### editor:reverse-sort-lines {{ selector: '.cm-editor' }}

Sorts the selected lines in reverse alphabetical order.

### editor:sort-lines-insensitive {{ selector: '.cm-editor' }}

Sorts the selected lines alphabetically, ignoring case.
Lines that differ only in case keep a stable order between themselves.

### editor:reverse-sort-lines-insensitive {{ selector: '.cm-editor' }}

Sorts the selected lines in reverse alphabetical order, ignoring case.

### editor:scroll-editor-to-line {{ selector: 'body' }}

Scrolls the editor to the given line.

<Properties sub title="Arguments">
  <Property name="line" type="number">
    Number, the destination line number.
  </Property>
</Properties>

### editor:scroll-preview-to-line {{ selector: 'body' }}

Scrolls the preview to the given line.

<Properties sub title="Arguments">
  <Property name="line" type="number">
    Number, the destination line number.
  </Property>
</Properties>

### editor:select-all {{ selector: '.cm-editor' }}

Selects all text in the editor.

### editor:select-lines-downward {{ selector: '.cm-editor' }}

Adds a cursor downward.

### editor:select-lines-upward {{ selector: '.cm-editor' }}

Adds a cursor upward.

### editor:single-selection {{ selector: '.cm-editor' }}

Removes all cursors except the primary one.

### editor:sync-editor-scroll {{ selector: 'body' }}

Syncs the scroll position of the editor with the same position of the preview pane.

### editor:sync-preview-scroll {{ selector: 'body' }}

Syncs the scroll position of the preview pane with the same position of the editor.

### editor:focus-title {{ selector: 'body' }}

Moves focus to the title input bar of the editor.

### editor:focus-and-go-doc-start {{ selector: 'body' }}

Focuses the editor and moves the cursor to the start of the document.

### editor:move-focus-to-next {{ selector: 'body' }}

Moves focus from the title input bar to the next editable area.

### editor:insert-text {{ selector: 'body' }}

Inserts the given text at the current cursor position.

<Properties sub title="Arguments">
  <Property name="text" type="string" required>
    The text to insert.
  </Property>
  <Property name="selection" type="object">
    (Optional) Selection to set after insertion, with `anchor` (number) and optional `head` (number) offsets from the insertion position.
  </Property>
</Properties>

### editor:open-link {{ selector: '.cm-editor' }}

Opens the URL at the cursor in the default browser.

### editor:copy-link {{ selector: '.cm-editor' }}

Copies the URL at the cursor to the clipboard.

### editor:paste-url {{ selector: '.cm-editor' }}

Pastes a URL at the cursor. If text is selected, wraps it as a Markdown link.

<Properties sub title="Arguments">
  <Property name="url" type="string" required>
    The URL to paste.
  </Property>
</Properties>

### editor:start-completion {{ selector: '.cm-editor' }}

Manually triggers the autocompletion popup.

### editor:accept-completion {{ selector: '.cm-editor' }}

Accepts the currently highlighted completion suggestion.

### editor:close-completion {{ selector: '.cm-editor' }}

Closes the autocompletion popup.

### editor:move-completion-selection-up {{ selector: '.cm-editor' }}

Moves the highlighted completion suggestion up.

### editor:move-completion-selection-down {{ selector: '.cm-editor' }}

Moves the highlighted completion suggestion down.

### editor:move-completion-selection-page-up {{ selector: '.cm-editor' }}

Moves the highlighted completion suggestion up by a page.

### editor:move-completion-selection-page-down {{ selector: '.cm-editor' }}

Moves the highlighted completion suggestion down by a page.

### editor:refresh {{ selector: 'body' }}

Requests the editor to recompute its measurements (after layout changes).

### editor:set-doc {{ selector: 'body' }}

Replaces the entire editor content with the given body.

<Properties sub title="Arguments">
  <Property name="body" type="string" required>
    The new document body.
  </Property>
</Properties>

### editor:toggle-comment {{ selector: '.cm-editor' }}

Comments or uncomments the selection, using line comments when the language provides them and block comments otherwise.
In Markdown text this produces an HTML comment (`<!-- -->`), since Markdown defines only block comment tokens.

### editor:toggle-line-comment {{ selector: '.cm-editor' }}

Comments or uncomments the selection using line comments.
This only takes effect inside a fenced code block whose language provides line comment tokens — in Markdown text it does nothing.

### editor:toggle-line-numbers {{ selector: 'body' }}

Toggles line numbers in the editor.

### editor:toggle-line-wrapping {{ selector: 'body' }}

Toggles soft line wrapping in the editor.

### editor:toggle-readable-line-length {{ selector: 'body' }}

Toggles a maximum readable line length in the editor.

### editor:toggle-limit-max-width {{ selector: 'body' }}

Toggles whether the editor limits its maximum content width.

### editor:add-code-language {{ selector: 'body' }}

Registers a code language with the Markdown editor for fenced-code syntax highlighting.

<Properties sub title="Arguments">
  <Property name="lang" type="LanguageDescription" required>
    A CodeMirror `LanguageDescription` instance.
  </Property>
</Properties>

### editor:remove-code-language {{ selector: 'body' }}

Removes a previously registered code language.

<Properties sub title="Arguments">
  <Property name="name" type="string" required>
    The name of the language to remove.
  </Property>
</Properties>

### editor:add-extension {{ selector: 'body' }}

Adds a CodeMirror extension to the editor.

<Properties sub title="Arguments">
  <Property name="extension" type="Extension" required>
    A CodeMirror `Extension`.
  </Property>
</Properties>

### editor:remove-extension {{ selector: 'body' }}

Removes a previously added CodeMirror extension.

<Properties sub title="Arguments">
  <Property name="extension" type="Extension" required>
    The extension to remove.
  </Property>
</Properties>

### editor:add-markdown-extension {{ selector: 'body' }}

Adds a Lezer Markdown extension to the parser.

<Properties sub title="Arguments">
  <Property name="extension" type="MarkdownExtension" required>
    A Lezer `MarkdownExtension`.
  </Property>
</Properties>

### editor:remove-markdown-extension {{ selector: 'body' }}

Removes a previously added Markdown extension.

<Properties sub title="Arguments">
  <Property name="extension" type="MarkdownExtension" required>
    The extension to remove.
  </Property>
</Properties>

### editor:add-slash-commands {{ selector: 'body' }}

Registers additional items for the slash-command menu.

<Properties sub title="Arguments">
  <Property name="commands" type="SlashCommandList" required>
    The slash commands to add.
  </Property>
</Properties>

### editor:remove-slash-commands {{ selector: 'body' }}

Unregisters previously added slash-command items.

<Properties sub title="Arguments">
  <Property name="commands" type="SlashCommandList" required>
    The slash commands to remove.
  </Property>
</Properties>

### editor:new-table-row-and-continue {{ selector: '.cm-editor' }}

Inserts a new row in the table at the cursor and continues editing.

### editor:move-to-next-table-cell {{ selector: '.cm-editor' }}

Moves the cursor to the next cell of the table at the cursor.

### editor:move-to-previous-table-cell {{ selector: '.cm-editor' }}

Moves the cursor to the previous cell of the table at the cursor.

### editor:toggle-task-list {{ selector: '.cm-editor' }}

Toggles task list to the current selections.

### editor:transpose-chars {{ selector: '.cm-editor' }}

Transposes characters at the cursor.

### editor:undo-selection {{ selector: '.cm-editor' }}

Undoes the last selection.

### editor:unindent {{ selector: '.cm-editor' }}

Unindents the current lines.

### editor:show-notes-in-book-of-editing-note {{ selector: 'body' }}

Display notes in a notebook of the currently editing note on the note list bar.

### editor:go-to-workspace-of-editing-note {{ selector: 'body' }}

Switches to the workspace of the notebook of the currently editing note.

---

## AI commands

Commands for managing the AI inline assistant. These are global commands you typically invoke programmatically (for example, from a plugin) to customize the built-in prompt presets shown when editing the selection with AI.

### ai:add-inline-assistant-prompt-presets {{ selector: 'body' }}

Adds prompt presets to the AI inline assistant. A preset whose ID matches an existing one replaces it; the rest are appended to the list.

<Properties sub title="Arguments">
  <Property name="presets" type="array" required>
    An array of preset objects, each with the following keys:

    <Properties sub>
      <Property name="id" type="string" required>
        A unique identifier for the preset.
      </Property>
      <Property name="label" type="string" required>
        The label shown in the preset list.
      </Property>
      <Property name="prompt" type="string" required>
        The prompt sent to the AI when the preset is selected.
      </Property>
    </Properties>

  </Property>
</Properties>

### ai:remove-inline-assistant-prompt-presets {{ selector: 'body' }}

Removes prompt presets from the AI inline assistant by ID.

<Properties sub title="Arguments">
  <Property name="ids" type="array" required>
    The IDs of the presets to remove.
  </Property>
</Properties>

---

## Table editor commands

These commands operate while the cursor is inside a Markdown table.

### table-editor:select-cell {{ selector: '.cm-editor' }}

Selects the contents of the current cell.

### table-editor:next-cell {{ selector: '.cm-editor' }}

Moves to the next cell.

### table-editor:previous-cell {{ selector: '.cm-editor' }}

Moves to the previous cell.

### table-editor:next-row {{ selector: '.cm-editor' }}

Moves to the next row.

### table-editor:escape {{ selector: '.cm-editor' }}

Exits the table editing mode.

### table-editor:move-left {{ selector: '.cm-editor' }}

Moves one cell to the left.

### table-editor:move-right {{ selector: '.cm-editor' }}

Moves one cell to the right.

### table-editor:move-up {{ selector: '.cm-editor' }}

Moves one cell up.

### table-editor:move-down {{ selector: '.cm-editor' }}

Moves one cell down.

### table-editor:align-left {{ selector: '.cm-editor' }}

Aligns the current column to the left.

### table-editor:align-right {{ selector: '.cm-editor' }}

Aligns the current column to the right.

### table-editor:align-center {{ selector: '.cm-editor' }}

Aligns the current column to the center.

### table-editor:align-none {{ selector: '.cm-editor' }}

Removes column alignment.

### table-editor:move-row-up {{ selector: '.cm-editor' }}

Moves the current row up.

### table-editor:move-row-down {{ selector: '.cm-editor' }}

Moves the current row down.

### table-editor:move-column-left {{ selector: '.cm-editor' }}

Moves the current column to the left.

### table-editor:move-column-right {{ selector: '.cm-editor' }}

Moves the current column to the right.

### table-editor:delete-column {{ selector: '.cm-editor' }}

Deletes the current column.

### table-editor:delete-row {{ selector: '.cm-editor' }}

Deletes the current row.

### table-editor:insert-column {{ selector: '.cm-editor' }}

Inserts a new column.

### table-editor:insert-row {{ selector: '.cm-editor' }}

Inserts a new row.

### table-editor:format {{ selector: '.cm-editor' }}

Reformats the current table.

### table-editor:format-all {{ selector: '.cm-editor' }}

Reformats all tables in the note.

---

## Export commands

### export-as-html:selections {{ selector: 'body' }}

Exports the selected notes on the note list bar as HTML to disk.
Exporting more than one note prompts for a destination directory; exporting exactly one opens a save dialog.

<Properties sub title="Arguments">
  <Property name="noteId" type="string">
    The note to export. Defaults to the notes selected on the note list bar, then to the editing note.
  </Property>
</Properties>

### export-as-html:notebook {{ selector: 'body' }}

Prompts for a directory and exports every note in a notebook, and its child notebooks, as HTML.

<Properties sub title="Arguments">
  <Property name="bookId" type="string">
    The notebook to export. Defaults to the notebook targeted by the context
    menu.
  </Property>
</Properties>

### export-as-html:copy {{ selector: 'body' }}

Copies a note to the clipboard as full templated HTML, with images inlined as data URIs.

<Properties sub title="Arguments">
  <Property name="noteId" type="string">
    The note to copy. Defaults to the first note selected on the note list bar.
  </Property>
</Properties>

### export-as-html:copy-simple {{ selector: 'body' }}

Copies a note to the clipboard as stripped-down HTML, without the document template.

<Properties sub title="Arguments">
  <Property name="noteId" type="string">
    The note to copy. Defaults to the first note selected on the note list bar.
  </Property>
</Properties>

### export-as-markdown:all {{ selector: 'body' }}

Exports all notes as Markdown to disk.

### export-as-markdown:selections {{ selector: 'body' }}

Exports the selected notes on the note list bar as Markdown, along with their attachments.
Exporting more than one note prompts for a destination directory; exporting exactly one opens a save dialog.

<Properties sub title="Arguments">
  <Property name="noteId" type="string">
    The note to export. Defaults to the notes selected on the note list bar, then to the editing note.
  </Property>
</Properties>

### export-as-markdown:notebook {{ selector: 'body' }}

Prompts for a directory and exports every note in a notebook, and its child notebooks, as Markdown.

<Properties sub title="Arguments">
  <Property name="bookId" type="string">
    The notebook to export. Defaults to the notebook targeted by the context
    menu.
  </Property>
</Properties>

### export-print:export-as-pdf {{ selector: 'body' }}

Exports the editing note as PDF to disk.

### export-print:print {{ selector: 'body' }}

Prints the editing note.

---

## Import commands

### import-html:import-from-file {{ selector: 'body' }}

Imports a note from a HTML file.

### import-markdown:import-from-file {{ selector: 'body' }}

Opens the Markdown import wizard, which scans the chosen files or directory and imports them into a notebook you pick.

---

## View commands

### view:sort-by-title-in-asc {{ selector: 'body' }}

Sorts the note list by title in ascending order.

### view:sort-by-title-in-desc {{ selector: 'body' }}

Sorts the note list by title in descending order.

### view:sort-by-date-created-in-asc {{ selector: 'body' }}

Sorts the note list by date created in ascending order.

### view:sort-by-date-created-in-desc {{ selector: 'body' }}

Sorts the note list by date created in descending order.

### view:sort-by-date-updated-in-asc {{ selector: 'body' }}

Sorts the note list by date updated in ascending order.

### view:sort-by-date-updated-in-desc {{ selector: 'body' }}

Sorts the note list by date updated in descending order.

### view:show-editor {{ selector: 'body' }}

Shows the editor pane.

### view:show-preview {{ selector: 'body' }}

Shows the preview pane.

### view:show-side-by-side {{ selector: 'body' }}

Shows the editor and preview side by side.

### view:toggle-distraction-free {{ selector: 'body' }}

Toggles distraction-free mode.

### view:toggle-preview {{ selector: 'body' }}

Toggles the preview pane.

### view:toggle-side-by-side {{ selector: 'body' }}

Toggles the side-by-side mode.

### view:toggle-sidebar {{ selector: 'body' }}

Toggles the side bar.

---

## Window commands

### window:close {{ selector: 'body' }}

Closes the window.

### window:decrease-font-size {{ selector: 'body' }}

Decreases the font size in the editor.

### window:increase-font-size {{ selector: 'body' }}

Increases the font size in the editor.

### window:new-inkdrop-window {{ selector: 'body' }}

Opens up a new window.

### window:reload {{ selector: 'body' }}

Reloads the window.

### window:toggle-dev-tools {{ selector: 'body' }}

Toggles the Developer Tools window.

### window:toggle-full-screen {{ selector: 'body' }}

Toggles full screen mode.

### window:toggle-menu-bar {{ selector: 'body', warning: 'Windows / Linux only' }}

Toggles the `core.autoHideMenuBar` config, which hides or shows the menu bar.
When the menu bar is hidden it can be revealed temporarily by pressing the Alt key.

---

## Special commands

The following commands are special for controlling the key event handler.

### native!

Triggers the default behavior of the key event.

### unset!

Unbinds the existing keybinding.

### abort!

Prevents the default behavior of the key event. It calls `preventDefault` on the event object.
