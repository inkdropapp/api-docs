export const metadata = {
  title: 'Environment',
  description: 'Inkdrop global for dealing with packages, themes, menus, and the window.',
}

# Environment

The top-level object representing the Inkdrop application in the renderer process — packages, themes, menus, the local database, and the window.
Your plugin receives it as the first argument of `activate(env)`.{{ className: 'lead' }}

```ts
import type { Environment, IInkdropPlugin } from '@inkdropapp/types'

class YourPlugin implements IInkdropPlugin {
  activate(env: Environment) {
    env.notifications.addInfo(`Running Inkdrop ${env.appVersion}`)
  }
  deactivate(env: Environment) {
    env.notifications.addInfo('Your plugin is deactivated')
  }
}

export default new YourPlugin()
```

The same instance is also exposed globally as `inkdrop`, but that global is deprecated and will be sandboxed away in a future release, so take `env` from `activate()` instead.

---

## Properties

<Properties>
  <Property name="appVersion" type="string">
    The application version string.
  </Property>
  <Property name="clipboard" type="object">
    The system clipboard proxy. See [IPCClipboard](https://github.com/inkdropapp/types/blob/main/src/ipc/clipboard.d.ts) for the available methods.
  </Property>
  <Property name="commands" type="object">
    A [CommandRegistry](/modules/command-registry) instance.
  </Property>
  <Property name="components" type="object">
    A [ComponentManager](/modules/component-manager) instance.
  </Property>
  <Property name="config" type="object">
    A [Config](/modules/config) instance.
  </Property>
  <Property name="contextMenu" type="object">
    A [ContextMenuManager](/modules/context-menu-manager) instance.
  </Property>
  <Property name="devMode" type="boolean">
    Boolean, true if Development Mode is enabled, false if disabled.
  </Property>
  <Property name="dialog" type="object">
    The native dialog box proxy. See [IPCDialog](https://github.com/inkdropapp/types/blob/main/src/ipc/dialog.d.ts) for the available methods.
  </Property>
  <Property name="keymaps" type="object">
    A [KeymapManager](/modules/keymap-manager) instance.
  </Property>
  <Property name="layouts" type="object">
    A [LayoutManager](/modules/layout-manager) instance.
  </Property>
  <Property name="localDB" type="object">
    The local database proxy, with [notes](/data-access/notes), [books](/data-access/books), [tags](/data-access/tags), [files](/data-access/files) and [utils](/data-access/utils) sub-objects plus [change events](/data-access/events). See [Accessing the local database](/guides/access-the-local-database).
  </Property>
  <Property name="markdownRenderer" type="object">
    A [MarkdownRenderer](/modules/markdown-renderer) instance.
  </Property>
  <Property name="menu" type="object">
    A [MenuManager](/modules/menu-manager) instance.
  </Property>
  <Property name="notifications" type="object">
    A [NotificationManager](/modules/notification-manager) instance.
  </Property>
  <Property name="packages" type="object">
    A [PackageManager](/modules/package-manager) instance.
  </Property>
  <Property name="resourcePath" type="string">
    A path to the app's bundled resource directory. For example, usually `"/Applications/Inkdrop.app/Contents/Resources/app.asar"` on macOS.
  </Property>
  <Property name="store" type="object">
    The [Redux store](https://redux.js.org/api/store) holding the [app state](/states). Use `store.getState()` to read it and `store.dispatch()` to run an [action](/actions) — see [Getting & modifying the app state](/guides/flux-architecture).
  </Property>
  <Property name="styles" type="object">
    A [StyleManager](/modules/style-manager) instance.
  </Property>
  <Property name="telescope" type="object">
    The Telescope manager for registering custom sources in the fuzzy finder.
  </Property>
  <Property name="themes" type="object">
    A [ThemeManager](/modules/theme-manager) instance.
  </Property>
  <Property name="userDataPath" type="string">
    The path to [the user's data directory](https://docs.inkdrop.app/reference/user-data-directory).
  </Property>
  <Property name="window" type="object">
    The IPC proxy for the current window.
  </Property>
</Properties>

---

## Editor Load Event {{ label: 'onEditorLoad(callback)' }}

Invoke the given callback when the editor has been loaded.

<Note>
  Prefer [`ensureEditorLoaded(callback)`](#ensure-editor-loaded), which also covers the case where an editor is already loaded by the time your plugin subscribes.
</Note>

<Row>
  <Col>
    ### Parameters
    <Properties>
      <Property name="callback(editor)" type="function" required>
        Function to be called when the editor loads, where `editor` is the [EditorView](https://codemirror.net/docs/ref/#view.EditorView) instance that was loaded.
      </Property>
    </Properties>

    ### Returns
    Returns a [Disposable](/event-subscription/disposable) on which `.dispose()` can be called to unsubscribe.
  </Col>
</Row>

---

## Editor Unload Event {{ label: 'onEditorUnload(callback)' }}

Invoke the given callback when the editor has been unloaded.

<Row>
  <Col>
    ### Parameters
    <Properties>
      <Property name="callback()" type="function" required>
        Function to be called when the editor unloads.
      </Property>
    </Properties>

    ### Returns
    Returns a [Disposable](/event-subscription/disposable) on which `.dispose()` can be called to unsubscribe.
  </Col>
</Row>

---

## Ensure Editor Loaded {{ label: 'ensureEditorLoaded(callback)' }}

Run the given callback with the active editor, waiting for one if needed.
If an editor is already loaded, the callback is invoked immediately; otherwise it is invoked once when the next editor loads, after which the subscription disposes itself.

<Row>
  <Col>
    ### Parameters
    <Properties>
      <Property name="callback(editor)" type="function" required>
        Function to be called with the loaded [EditorView](https://codemirror.net/docs/ref/#view.EditorView).
      </Property>
    </Properties>

    ### Returns
    Nothing. In windows without an editor the callback simply never fires rather than throwing.
  </Col>
</Row>

---

## App Readiness Event {{ label: 'onAppReady(callback)' }}

Invoke the given callback when the app is ready.

<Row>
  <Col>
    ### Parameters
    <Properties>
      <Property name="callback()" type="function" required>
        Function to be called when the app is ready.
      </Property>
    </Properties>

    ### Returns
    Returns a [Disposable](/event-subscription/disposable) on which `.dispose()` can be called to unsubscribe.
  </Col>
</Row>

---

## App Quit Event {{ label: 'onAppQuit(callback)' }}

Invoke the given callback when the app is about to quit.

<Row>
  <Col>
    ### Parameters
    <Properties>
      <Property name="callback()" type="function" required>
        Function to be called before the app quits.
      </Property>
    </Properties>

    ### Returns
    Returns a [Disposable](/event-subscription/disposable) on which `.dispose()` can be called to unsubscribe.
  </Col>
</Row>

---

## Is Editor Active {{ label: 'isEditorActive()' }}

Check if the editor is currently active.

<Row>
  <Col>
    ### Returns
    Returns a Boolean indicating whether the editor is active.
  </Col>
</Row>

---

## Get Active Editor {{ label: 'getActiveEditor()' }}

Retrieve the active editor instance.

<Row>
  <Col>
    ### Returns
    Returns the active [EditorView](https://codemirror.net/docs/ref/#view.EditorView) if the editor is active, otherwise `null` or `undefined`.
  </Col>
</Row>

---

## Get Active Editor with Error Handling {{ label: 'getActiveEditorOrThrowError()' }}

Retrieve the active editor instance or throw an error if it's not available.

<Row>
  <Col>
    ### Returns
    Returns the active [EditorView](https://codemirror.net/docs/ref/#view.EditorView) if the editor is active, otherwise throws an error.
  </Col>
</Row>

---

## Set Active Editor {{ label: 'setActiveEditor(editor)' }}

Set the currently active editor instance.

<Row>
  <Col>
    ### Parameters
    <Properties>
      <Property name="editor" type="EditorView" required>
        The [EditorView](https://codemirror.net/docs/ref/#view.EditorView) to set as the active editor, or `null` to clear it.
      </Property>
    </Properties>
  </Col>
</Row>

---
